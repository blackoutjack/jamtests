function runTest() {
  var v0=Math.random();introspect(JAM.policy.p2)x=3*v0;x=4;var v1=Math.random();introspect(JAM.policy.p2)x=3*v1;x=5;var v2=Math.random();introspect(JAM.policy.p2)x=3*v2;x=6

  return x === 6;
}
